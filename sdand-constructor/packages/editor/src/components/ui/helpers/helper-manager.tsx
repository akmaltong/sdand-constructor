'use client'


export function HelperManager() {
  // Sdand: скрываем все hint-панели (Начало/конец стены, Esc, R/T…) —
  // на демо заказчику интерфейс должен быть чистый, без «обучающих» оверлеев.
  return null
}
