'use client';
// Sdand: спавн-флажок скрыт из сцены целиком (registry-layer overlay
// игнорирует `visible={false}` на группе, поэтому проще возвращать null).
const SpawnRenderer = (_props) => null;
export default SpawnRenderer;
